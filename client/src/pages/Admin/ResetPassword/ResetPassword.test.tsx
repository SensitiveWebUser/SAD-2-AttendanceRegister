ResetPassword;
import { ResetPassword } from './ResetPassword';

describe('<ResetPassword />', () => {
  test('Should render the ResetPassword component', async () => {
    <ResetPassword />;
  });
});
